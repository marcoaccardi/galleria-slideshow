import React from "react";

const Header = () => {
  return (
    <header>
      <nav>
        <a href='/'>galleria.</a>
        <button>start slideshow</button>
      </nav>
    </header>
  );
};

export default Header;
